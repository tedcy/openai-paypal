# Optional dependency for local Headless Playwright fingerprint/DataDome/MTR modes.
# After installing this file, run: python -m playwright install chromium
-r requirements.txt
playwright
curl_cffi==0.13.0
